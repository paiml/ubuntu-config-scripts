// Generated by TypeScript-Ruchy Bridge Transformer
// Original TypeScript: test-bridge-example.ts
// Target Ruchy version: 0.10.0
// Transformation date: 2025-08-23T21:59:43.918Z
//
// Note: This is an automated transformation. Manual review required.

#!/usr/bin/env -S deno run --allow-all

/**
 * Simple TypeScript example for bridge testing
 */

pub struct Config {
  name;
  version;
  enabled;
}

pub fun greet(name): string {
  return format!("Hello, {}!", /* add variables here */);
}

pub fun calculateSum|a, b| {
  return a + b;
};

async fun main() {
  let config= {
    name: "Bridge Test",
    version: 1,
    enabled: true
  };

  println!(greet(config.name));
  println!(format!("Sum: {}", /* add variables here */));
  
  let numbers = [1, 2, 3, 4, 5];
  let doubled = numbers.iter().map(n => n * 2);
  
  println!("Doubled:", doubled);
}

if (import.meta.main) {
  await main();
}